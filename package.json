{
  "name": "kyc-ocr-extractor",
  "version": "1.0.0",
  "description": "Extrae información de documentos PDF usando OCR con IA.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [
    "ocr",
    "ai",
    "pdf",
    "extractor",
    "nodejs"
  ],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3", // Loads environment variables from .env file
    "express": "^4.18.2", // Web framework
    "multer": "^1.4.5-lts.1", // Middleware for handling file uploads
    "openai": "^4.0.0", // OpenAI API client library
    "pdf-img-convert": "^1.2.1", // Library to convert PDF pages to images (uses canvas)
    "uuid": "^9.0.0" // For generating unique IDs if needed
  },
  "devDependencies": {
    "nodemon": "^2.0.20" // Utility to automatically restart the server during development
  }
}
